/* action creators that go to the reducer */

export const getNodeNames = (nodeNames) => ({
  // return (dispatch) => {
  //   dispatch({
      type: actionTypes.GET_NODE_NAMES,
      payload: nodeNames
    // });
  // }
});
